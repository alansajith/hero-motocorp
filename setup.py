"""Setup file for vehicle damage detection package."""

from setuptools import setup, find_packages

setup(
    name="vehicle-damage-detection",
    version="1.0.0",
    description="AI-powered vehicle damage detection and assessment system",
    author="Alan Sajith",
    packages=find_packages(),
    python_requires=">=3.9",
    install_requires=[
        # Read from requirements.txt
    ],
)
